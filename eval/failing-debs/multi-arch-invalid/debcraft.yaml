name: multi-arch-invalid
version: "1.0"
summary: Package with invalid multi-arch value
description: |
  multi-arch must be: no, same, foreign, or allowed.

maintainer: Test User <test@example.com>

packages:
  multi-arch-invalid:
    multi-arch: yes

parts:
  my-part:
    plugin: nil
