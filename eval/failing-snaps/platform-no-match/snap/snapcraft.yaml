name: platform-no-match
version: "1.0"
summary: test
description: |
  Snap with explicit platform definitions for amd64 and arm64 targets.
confinement: strict
base: core24
platforms:
  platform1:
    build-on: amd64
    build-for: amd64
  platform2:
    build-on: [amd64, arm64]
    build-for: arm64

parts:
  nil:
    plugin: nil
